$('#vote_btn_' + <%= @counseling_post.id %>).html("<%= j(render 'public/votes/vote', counseling_post: @counseling_post, vote: @vote, user_vote: @user_vote, keep_votes: @keep_votes, either_votes: @either_votes, discard_votes: @discard_votes) %>");
$('.vote-message').html("<%= j(@message) %>").addClass('green-background').show().delay(3000).fadeOut();
