<% if counseling_post.favorited_by?(current_user) %>
  <%= link_to counseling_post_favorites_path(counseling_post.id), method: :delete, class: "btn btn-danger" do %>
    ためになる（<%= counseling_post.favorites.count %>）
  <% end %>
<% else %>
  <%= link_to counseling_post_favorites_path(counseling_post.id), method: :post, class: "btn btn-primary" do %>
    ためになる（<%= counseling_post.favorites.count %>）
  <% end %>
<% end %>